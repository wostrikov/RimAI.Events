# RimAI.Events

Composition/lifecycle reference for RimAI product modules (`EventsMod` + `EventsComposition`).

`Source/` remains intentionally flat — no empty Domain/UI/Patches folders.
See repository convention: `docs/architecture/rimai-module-structural-convention.md`.
